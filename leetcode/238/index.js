var productExceptSelf = function(nums) {
  let res = [];

  nums.forEach((el, i) => {
    res.push(prod);
  });

  return res;
};

const test = [1, 2, 3, 4];
console.log(productExceptSelf(test)); //[24,12,8,6]
